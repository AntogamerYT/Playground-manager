module.exports = {
    name: 'pgsettings',
    aliases: ['pgs', 'pm', 'playgroundsettings', 'playgroundmanagment'],
    async execute(client, message, args) {
        message.channel.send('Work in progress!')
// still have to do this
    }
}